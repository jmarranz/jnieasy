
#include "Util.h"

/*

#include <stdio.h>

#ifdef WINDOWS
	#include <windows.h>
#endif

void showNumber(jint num)
{
	char s[256];
	sprintf(s,"%d",num);
#ifdef WINDOWS
	MessageBox(NULL,s, s,0);
#endif
	printf(s);
}

void showNumber(const char* pattern,jint num)
{
	char s[256];
	sprintf(s,pattern,num);
#ifdef WINDOWS
	MessageBox(NULL,s, s,0);
#endif
	printf(s);
}

void showMessage(const char* msg)
{
#ifdef WINDOWS
	MessageBox(NULL,msg,msg,0);
#endif
	printf(msg);
}

*/



